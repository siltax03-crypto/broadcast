# Broadcast Message Extension for SillyTavern

여러 채팅에 동일한 메시지를 전송하고, 전송 후 자동으로 숨김 처리하는 SillyTavern 확장 프로그램입니다.

## 📦 설치 방법

1. SillyTavern 설치 폴더로 이동합니다.
2. `public/scripts/extensions/third-party/` 폴더를 찾습니다.
3. 이 `broadcast-message` 폴더 전체를 해당 위치에 복사합니다.

```
SillyTavern/
└── public/
    └── scripts/
        └── extensions/
            └── third-party/
                └── broadcast-message/    <-- 여기에 복사
                    ├── manifest.json
                    ├── index.js
                    ├── style.css
                    └── README.md
```

4. SillyTavern을 재시작합니다.

## 🚀 사용 방법

### UI 버튼 사용
1. SillyTavern 우측 상단의 확장 메뉴(퍼즐 아이콘)를 클릭합니다.
2. "브로드캐스트" 버튼을 클릭합니다.
3. 메시지를 보낼 채팅들을 선택합니다.
4. 메시지를 입력합니다.
5. "자동 숨김" 옵션을 원하면 체크합니다.
6. "전송" 버튼을 클릭합니다.

### 슬래시 커맨드 사용
채팅창에 `/broadcast`를 입력하면 브로드캐스트 UI가 열립니다.

## ⚙️ 기능

- **다중 채팅 선택**: 체크박스로 여러 채팅을 선택할 수 있습니다.
- **전체 선택**: 한 번에 모든 채팅을 선택/해제할 수 있습니다.
- **자동 숨김**: 전송된 메시지와 AI 응답을 자동으로 `/hide` 처리합니다.
- **진행 상태 표시**: 전송 진행 상황을 토스트 메시지로 알려줍니다.

## ⚠️ 주의사항

1. **SillyTavern 버전**: 이 확장 프로그램은 SillyTavern 1.11.x 이상에서 테스트되었습니다. 
   버전에 따라 일부 기능이 다르게 동작할 수 있습니다.

2. **API 호환성**: 채팅 전환 및 메시지 전송 방식은 SillyTavern 내부 API에 의존합니다.
   업데이트로 인해 동작이 변경될 수 있습니다.

3. **딜레이 설정**: 기본적으로 각 채팅 간 2초의 딜레이가 있습니다.
   `index.js`의 `delayBetweenChats` 값을 수정하여 조절할 수 있습니다.

## 🔧 커스터마이징

### 딜레이 조절
`index.js`에서 아래 값을 수정하세요:
```javascript
const defaultSettings = {
    autoHide: true,
    delayBetweenChats: 2000, // 밀리초 단위 (2000 = 2초)
};
```

### 숨김 동작 수정
현재는 마지막 2개 메시지(보낸 메시지 + 응답)를 숨깁니다.
`handleMessageReceived()` 함수를 수정하여 동작을 변경할 수 있습니다.

## 🐛 문제 해결

### 채팅 목록이 표시되지 않는 경우
- SillyTavern의 API 엔드포인트가 변경되었을 수 있습니다.
- 브라우저 개발자 도구(F12)의 콘솔에서 에러를 확인하세요.

### 메시지가 전송되지 않는 경우
- 채팅 전환이 완료되기 전에 메시지가 전송될 수 있습니다.
- `delayBetweenChats` 값을 늘려보세요.

### 숨김이 동작하지 않는 경우
- `/hide` 슬래시 커맨드가 활성화되어 있는지 확인하세요.
- 일부 SillyTavern 버전에서는 숨김 기능의 구현이 다를 수 있습니다.

## 📝 라이선스

MIT License - 자유롭게 수정하고 배포할 수 있습니다.

## 🤝 기여

버그 리포트나 기능 제안은 언제든 환영합니다!
